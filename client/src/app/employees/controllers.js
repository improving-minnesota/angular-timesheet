angular.module('app.employees.controllers', [])
  
  .controller('EmployeeCtrl', 
    function (data) {
      var vm = this;

      // TODO : implement a function on controller (vm) to request the list of employees
      // TODO : implement a function on controller (vm) to remove an employee
      // TODO : implement a function on controller (vm) to restore an employee
      // TODO : initialize controller (vm) by calling requestEmployees
      
    }
  );